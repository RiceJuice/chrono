import 'package:flutter/material.dart';

ImageProvider? resolveCalendarEntryDiskImageProvider(Object file) => null;

Widget buildCalendarLocalDiskImage({
  required Object file,
  required BoxFit fit,
  required Widget error,
}) {
  return error;
}
