# Lokale Referenz für Push-Webhook (NICHT committen).
# Kopieren nach: config/push_secrets.local.env
# Gleiche Werte zusätzlich in Supabase Edge Function Secrets setzen.

# Selbst generieren (siehe backend/PUSH_NOTIFICATIONS.md)
N8N_WEBHOOK_SECRET=ersetze-mit-deinem-geheimen-string

# Optional: nur für lokale curl-Tests, nie in die Flutter-App
# SUPABASE_PROJECT_REF=chrbvfaknykaycwumuba
